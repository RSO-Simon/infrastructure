apiVersion: v1
kind: Service
metadata:
  name: component-service
spec:
  selector:
    app.kubernetes.io/name: component-service
    app.kubernetes.io/instance: component-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
