ingressClassName: nginx
host: ""

paths:
  ships:
    prefix: /
    serviceName: ship-service
    servicePort: 8081

  components:
    prefix: /api/components
    serviceName: component-service
    servicePort: 8082
